codecov:
  require_ci_to_pass: yes


coverage:
  status:
    project:
      default:
        target: 50%
        branches: [dev, main]
    patch:
      default:
        target: 50%
        branches: [dev, main]

ignore:
  
"frontend/tests/"